import 'package:flutter/material.dart';

/// Logical canvas width/height for responsive scaling (see `ScreenUtilInit`).
abstract final class DesignSize {
  static const Size mobile = Size(390, 844); // logical design canvas
}
